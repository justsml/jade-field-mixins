//- Base field with a label
//- 
//- Used inside '.form-horizontal' to display a label left of the textarea
//- 
//- Accepted arguments:
//- * title (String) - Add label above field, full width
//- * label (String)
//- * textarea args (name, value, placeholder)

include textarea

mixin labelled-textarea(args)
	.form-group
		if args.title
			label(for=attributes.id ? attributes.id : null).col-xs-12= args.title
		if args.label
			label(for=attributes.id ? attributes.id : null).col-sm-3= args.label
			.col-sm-9
				+textarea(args)(id=attributes.id,class=attributes.class)
		else
			.col-xs-12
				+textarea(args)(id=attributes.id,class=attributes.class)	